@model ProyectoCore.Models.ViewModels.ListaDeseoVM
@{
    ViewData["Title"] = "ListaDeseo_Detalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model.oListaDeseo.IdLista == 0)
{

    <h1>Crear Lista Deseo</h1>
}
else
{
    <h1>Editar Lista Deseo</h1>
}


<div class="row">
    <div class="col-sm-4">

        <form asp-action="ListaDeseo_Detalle" asp-controller="Home" method="post">
            <input type="hidden" asp-for="oListaDeseo.IdLista" />

            <div class="mb-3">
                <label class="form-label" asp-for="oListaDeseo.IdUsuario">Nombre Usuario</label>
                <select class="form-select form-select-sm" asp-for="oListaDeseo.IdUsuario" asp-items="@Model.oListaUsuario">
                    <option selected disabled>-- Seleccionar --</option>
                </select>
            </div>

            <div class="mb-2">

                @if (Model.oListaDeseo.IdLista == 0)
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Crear</button>
                }
                else
                {
                    <button class="btn btn-primary btn-sm w-25" type="submit">Actualizar</button>

                }
                <a class="btn btn-dark btn-sm" asp-action="IndexListaDeseo" asp-controller="Home">Volver</a>

            </div>

        </form>

    </div>
</div>


