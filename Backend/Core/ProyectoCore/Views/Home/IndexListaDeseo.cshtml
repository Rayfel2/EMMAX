@model List<ProyectoCore.Models.ListaDeseo>
@{

    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Lista deseo</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div class="card">
    <div class="card-header">
        <h5>Lista de Deseo</h5>
    </div>
    <div class="card-body">
        <a class="btn btn-success btn-sm" asp-action="ListaDeseo_Detalle" asp-controller="Home" asp-route-idListaDeseo="0">Crear Nuevo</a>
        <hr />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Usuario de la lista de deseo</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.oUsuario?.NombreUsuario</td>
                        <td>
                            <a class="btn btn-primary btn-sm " asp-action="ListaDeseo_Detalle" asp-controller="Home" asp-route-idListaDeseo="@item.IdLista"> Editar</a>
                            <a class="btn btn-danger btn-sm " asp-action="Eliminar_ListaDeseo" asp-controller="Home" asp-route-idListaDeseo="@item.IdLista"> Eliminar</a>
                        </td>
                    </tr>



                }
            </tbody>
        </table>


    </div>
</div>

